#include <iostream>
using namespace std;

int main() {
    double a, b, c;
    cout << "Ingrese tres números: ";
    cin >> a >> b >> c;
    cout << "Promedio = " << (a + b + c) / 3;
    return 0;
}
