#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double base, exponente;
    cout << "Base y exponente: ";
    cin >> base >> exponente;
    cout << "Resultado = " << pow(base, exponente);
    return 0;
}
